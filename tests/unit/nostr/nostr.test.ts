/**
 * This file contains a set of unit tests for nostr related functions like getBlankEvent(),
 * serializeEvent(), getEventHash(), validateEvent(), verifySignature() and signEvent(). It tests these
 * functions with an array of filters and an event object as parameters to check if any of them match
 * each other or not.
 */
