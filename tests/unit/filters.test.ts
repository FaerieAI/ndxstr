/**
 * This file contains a set of unit tests for filter related functions like matchFilters() which takes
 * an array of filters and an event object as parameters to check if any of them match each other or
 * not.
 */
