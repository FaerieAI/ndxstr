/**
 * This file contains a set of integration tests for database related functions like insert() and
 * client(). It also checks if Knex client connection is established properly or not before running any
 * queries against it.
 */
